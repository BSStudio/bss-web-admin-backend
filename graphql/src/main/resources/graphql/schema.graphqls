type Query {
    bookById(id: ID): Book,
    books: [Book],
}

type Book {
    id: ID
    name: String
    pageCount: Int
    authorId: ID
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}
