import addEvents from "./add-events";
import addMembers from "./add-members";
import addVideos from "./add-videos";
import truncateAll from "./truncate-all";

export const dbUtils = {
    addEvents,
    addMembers,
    addVideos,
    truncateAll,
}
